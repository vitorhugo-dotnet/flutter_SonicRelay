# SonicRelay Dark UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the reusable dark Material 3 UI foundation and four presentation-only screens requested by issue #2.

**Architecture:** Semantic theme tokens feed small shared widgets, which compose responsive feature pages. Navigation demonstrates the shell locally while all integration boundaries remain untouched.

**Tech Stack:** Flutter, Material 3, go_router, flutter_test

---

### Task 1: Specify the presentation contract with widget tests

**Files:**
- Modify: `test/app/sonic_relay_app_test.dart`
- Create: `test/core/widgets/sonic_widgets_test.dart`

- [ ] Add tests for the dark Material 3 theme, login-to-join navigation, all route content, small-phone layout, button busy state, badge state, and loading overlay.
- [ ] Run `flutter test test/app/sonic_relay_app_test.dart test/core/widgets/sonic_widgets_test.dart` and confirm failure because the requested UI and widgets do not exist.

### Task 2: Implement theme tokens and reusable widgets

**Files:**
- Create: `lib/app/theme/app_colors.dart`
- Create: `lib/app/theme/app_spacing.dart`
- Create: `lib/app/theme/app_typography.dart`
- Modify: `lib/app/theme/app_theme.dart`
- Create: `lib/core/widgets/sonic_button.dart`
- Create: `lib/core/widgets/sonic_card.dart`
- Create: `lib/core/widgets/sonic_text_field.dart`
- Create: `lib/core/widgets/connection_badge.dart`
- Create: `lib/core/widgets/loading_overlay.dart`

- [ ] Define semantic dark palette, spacing constants, and typography.
- [ ] Assemble a dark Material 3 theme with themed controls and cards.
- [ ] Implement the five focused reusable widgets with const-friendly APIs and accessible Material behavior.

### Task 3: Compose the four responsive pages

**Files:**
- Modify: `lib/app/sonic_relay_app.dart`
- Modify: `lib/features/auth/presentation/login_page.dart`
- Modify: `lib/features/sessions/presentation/join_session_page.dart`
- Modify: `lib/features/listener/presentation/listener_page.dart`
- Modify: `lib/features/settings/presentation/settings_page.dart`
- Delete: `lib/core/widgets/feature_scaffold.dart`

- [ ] Apply the dark theme at app bootstrap.
- [ ] Build each requested screen from the shared widgets, using scrollable constrained layouts.
- [ ] Add presentation-only navigation callbacks without services or business logic.
- [ ] Run the two focused widget-test files and confirm they pass.

### Task 4: Document and verify

**Files:**
- Modify: `README.md`

- [ ] Add a concise UI overview and screenshot capture instructions.
- [ ] Run `dart format` on changed Dart files.
- [ ] Run `flutter analyze` and confirm zero issues.
- [ ] Run `flutter test` and confirm all tests pass.
- [ ] Review `git diff --check` and the scoped diff, then commit, push `main`, and close issue #2 with a summary.

name: Android Play Deploy Fixed

on:
  workflow_dispatch:

jobs:
  test-fastlane-install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.4"
      - run: gem install fastlane --no-document
